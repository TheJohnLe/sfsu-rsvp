# Database

Please visit the `iassist-tables.sql` file to view the available tables and attributes.

Optionally use the `iassist-sample-data.sql` as a template to running a sample data query. You will still need to mandatorily update the Unix Epoch Timestamps in order to have usable test data.
